<%- model_class = Oculiste -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <!--<th><%= model_class.human_attribute_name(:id) %></th>-->
      <th><%= model_class.human_attribute_name(:rdv) %></th>
      <th><%= model_class.human_attribute_name(:note) %></th>
      <!--<th><%= model_class.human_attribute_name(:user_id) %></th>-->
      <!--<th><%= model_class.human_attribute_name(:created_at) %></th>-->
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @oculistes.each do |oculiste| %>
      <tr>
        <!--<td><%= link_to oculiste.id, oculiste_path(oculiste) %></td>-->
        <td><%= oculiste.rdv %></td>
        <td><%= oculiste.note %></td>
        <!--<td><%= oculiste.user_id %></td>-->
        <!--<td><%=l oculiste.created_at %></td>-->
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_oculiste_path(oculiste), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      oculiste_path(oculiste),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_oculiste_path,
            :class => 'btn btn-primary' %>
